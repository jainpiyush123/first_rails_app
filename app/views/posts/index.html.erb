<center>
<div class="container" style="background-color: #FDF7E4">
<h1>View All Posts</h1>
<table class="table table-hover">
    <thead>
      <tr>
        <th>S.No</th>
        <th>Title</th>
        <th>Body</th>
      </tr>
    </thead>
    <tbody>
      <% @posts.each_with_index do |post, index| %>
  <tr>
    <td><%= index+1 %></td>
    <td><%= post.title %></td>
    <td> <%= truncate(post.body, :length => 50) %></td>
    <td><%= link_to 'View', post_path(post) %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Delete', post_path(post), method: :delete, data: {confirm: "Do you really want to delete it?"} %></td>
  </tr>
  <% end %>
    </tbody>
  </table>
  </div>
  </center>

  <script>
	  $("tr[data-link]").click(function() {
	  window.location = $(this).data("link")
	})
  </script>
